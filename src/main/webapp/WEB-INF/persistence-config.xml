<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location" value="classpath:persistance.properties"/>
    </bean>

    <!-- нужен неймспейс контекст для плейсхолдера:
    <context:property-placeholder location="classpath:persistance.properties"/>
    -->

    <!-- connection via jdbc driver:
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName"
        <property name="url"
        <property name="username"
        <property name="password"
    </bean>

    <bean jdbc template...
    <bean class= session local factory bean....

    <bean jpa vendor adapter....(showaql, generateddl)
    <bean class= entity manager factory bean....
    <bean jpa transaction manager....
    <tx:annotation-driven/>
    -->



</beans>